/**
 * 旧既定 LLM → 同梱モデルへ切り替えたときに一度だけ出す通知の文言。
 * **通知の文言はすべてここに置く**(コンポーネントに直書きしない)。
 *
 * ⚠️ **文言は暫定**。「利用者にとって何が変わるか」は、軽量モデルでの添削機能を
 * まだ評価中なので確定していない。評価が終わるまでは
 * 「返答が短くなり、日本語訳が安定する」ことだけを言い、
 * 添削や単語カードについては何も約束しないこと。
 *
 * モデル名を変えたら bundled-llm-migration.test.ts が落ちる
 * (同梱モデルのカタログ表示名と旧既定のタグが文中にあるかを見ている)。
 */
export const BUNDLED_LLM_NOTICE = {
  title: '会話の AI を Qwen 2.5 1.5B に切り替えました',
  reason:
    'これまでの Llama 3.2 3B は日本語訳が不安定だったため、アプリに同梱の Qwen 2.5 1.5B に変更しました。',
  changes: '返答は短めになり、日本語訳がより安定します。',
  howToRevert: '元に戻すには、設定の「LLM」で llama3.2:3b を選んでください。',
  openSettings: '設定を開く',
  dismiss: '閉じる',
} as const
